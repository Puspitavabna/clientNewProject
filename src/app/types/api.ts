// Interface for the homepage data
export interface HomepageData {
  title: string;
  content: string;
}

// Interface for the service data
export interface ServiceData {
  id: number;
  name: string;
  description: string;
}

// Interface for the employee data
export interface EmployeeData {
  id: number;
  name: string;
  role: string;
}
